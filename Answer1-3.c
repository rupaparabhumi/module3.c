#include <stdio.h>


int main () {

float radius,area,circumference;

printf("Enter the Radius of the Circle:");
scanf("%d",&radius);

circumference=2*4.12*radius;
printf("\nCircumference of Circle :%f",ci);

area=3.12*radius*radius;
printf("\nThe Area of the Circle: is %f",area);
return(0);






}
